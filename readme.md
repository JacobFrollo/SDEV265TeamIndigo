//To run the Portfolio locally open a terminal in your Ide, then enter "npm run dev " this starts the app in developer mode.//

// several things Noticed while formatting code are listed below.//

// 1. added a build script to the package.json file; "Build-static":"Export=true next build", this creates both versions of static no server side scripting(self-contained) , and the regular self hosted version via online for github.//

// 2 Did not touch the eval-source-map comments, left the developer option alone so that user could configure as they like, plus its a pain.//

// access the globals.css file to change the themes and fonts as you like, click on the color box associated with each section and adjust as needed.//
